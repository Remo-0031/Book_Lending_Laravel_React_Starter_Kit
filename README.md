A simple Book Lending System built using Laravel and React (Inertia.js). It supports basic CRUD for book lending transactions with students.

## Test Data for Students

php artisan db:seed --class=StudentSeeder
